# Motal Battle 개발 로드맵 (정리본)

이 문서는 **향후 구현 계획(Plan)** 문서다.  
실제 구현 완료/진행 상태는 `docs/IMPLEMENTATION_STATUS.md`를 단일 기준으로 관리한다.

---

## 0) 프로젝트 핵심 원칙

1. **Grid = Rule, Phaser = View**
   - 보드 좌표/점유/전투 판정은 코어 규칙 레이어에서만 확정한다.
   - Scene/애니메이션은 상태를 표현만 하며, 규칙 진실(source of truth)이 아니다.

2. **드래그 UX와 규칙 판정을 분리한다**
   - 입력은 자유 드래그처럼 보이되, 실제 판정은 격자 단위로 수행한다.
   - 드래그 중 프리뷰와 최종 확정(릴리즈/타이머)을 분리한다.

3. **데이터 우선 설계**
   - 스테이지/시나리오/대화/유닛은 JSON 스키마 기반으로 외부화한다.
   - 하드코딩된 규칙은 점진적으로 데이터 규칙으로 이전한다.

---

## 1) 목표 아키텍처

- `src/core`: 순수 규칙 엔진 (보드, 이동/충돌/전투/하자드, 로더)
- `src/phaser`: 렌더링/입력/연출/UI
- `src/data`: 스테이지/시나리오/대화/유닛/스키마

### 설계 가드레일
- 코어 규칙은 Phaser 의존 금지
- 테스트는 core 규칙에 집중
- Scene은 규칙 결과를 소비하고 시각화만 담당

---

## 2) 단계별 로드맵

## Phase A. 코어 안정화 (규칙 완성도)

### A-1. 이동/충돌 규칙 보강
- 밀림(연쇄) 경계 케이스 추가 검증
- swap/block/none 전이 조건 표준화
- 상태(`stunned`, `hasActed`)와 드래그 판정 일관성 고정

### A-2. 턴/전투 후처리 정리
- 전투 → 후속효과(hazard) 순서 확정 유지
- 연쇄 전투/협공 중복 트리거 방지 규칙 문서화

### A-3. 테스트 확장
- resolver 단위 테스트를 케이스 기반으로 증설
- 회귀 시나리오(과거 버그) 테스트 고정

---

## Phase B. 스테이지 목표/진행 일반화

### B-1. 목표 시스템 구현
- 현재 적 전멸 고정 조건에서 벗어나 objective 타입 확장
  - 예: `defeat_all`, `survive_n_turns`, `reach_cell`, `protect_unit`
- 스테이지 성공/실패 판정기를 코어 레이어로 분리

### B-2. 시나리오 진행 분기 확장
- step 성공/실패 결과에 따른 분기(`onSuccess`, `onFail`) 지원
- retry/next/title 흐름을 공통 상태머신으로 단순화

### B-3. 데이터 스키마 갱신
- stage/scenario 스키마에 목표/분기 필드 정식 반영
- 검증 실패 메시지 가독성 개선

---

## Phase C. UX/연출 고도화

### C-1. 대화 UI 품질 개선
- 로그/스킵/자동재생/타이핑 등 읽기 UX 옵션화
- 모바일 입력(탭/롱프레스) 대응

### C-2. 전투/이동 피드백 강화
- swap, sandwich, 피격/사망 연출 레이어 정리
- 정보 과밀 방지를 위한 타이밍 조절(큐/우선순위)

### C-3. 화면/해상도 품질
- 2560x1440 기준 UI 앵커 재점검
- 고해상도 자산에서 성능 저하 구간 프로파일링

---

## Phase D. 콘텐츠 제작 파이프라인

### D-1. 콘텐츠 템플릿 정립
- 스테이지/대화/시나리오 작성 템플릿 제공
- 밸런스 튜닝 파라미터 표준화 (HP/ATK/DEF/하자드)

### D-2. 플레이 루프 확장
- 다중 시나리오 묶음(챕터 단위) 운영 구조 추가
- 튜토리얼/중반/후반 난이도 곡선 설계

---

## 3) 완료 기준(Definition of Done)

각 항목은 아래를 만족해야 완료로 본다.

1. 코어 규칙 단위 테스트 통과
2. 빌드 성공
3. 데이터 스키마 검증 통과
4. `docs/IMPLEMENTATION_STATUS.md` 동기화 완료

---

## 4) 현재 기준 권장 Next Focus

현 시점 우선순위는 아래 순서를 권장한다.

1. **스테이지 목표 시스템 일반화 (Phase B-1)**
2. **다중 스테이지 진행 분기 정리 (Phase B-2)**
3. **대화/플로우 오버레이 UX 고도화 (Phase C-1)**

> 실제 착수/완료 상태는 `docs/IMPLEMENTATION_STATUS.md`의 `Next Action`을 최종 기준으로 한다.
